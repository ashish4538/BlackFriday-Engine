apiVersion: v1
kind: Service
metadata:
  name: {{ include "flash-sale.fullname" . }}
  labels:
    {{- include "flash-sale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flash-sale.selectorLabels" . | nindent 4 }}
